Frontend scaffold (React + Vite + Tailwind + Leaflet)

Quick start:

1. cd frontend
2. npm install
3. npm run dev

Dev server proxies `/api` to `http://localhost:4000` and `/ml` to `http://localhost:8000` (see `vite.config.js`).
